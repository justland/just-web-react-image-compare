name: pull-request
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
  merge_group:

jobs:
  code:
    uses: justland/.github/.github/workflows/pnpm-verify.yml@main
    with:
      os: '["ubuntu-latest"]'
